describe("Spinner", function(){

  it("is going to show on Document Load", function(){
    onReady();
    expect(spinnerVisible).toBe(true);
  });

  it("is going to hide when Get is finished", function(){

  });
});